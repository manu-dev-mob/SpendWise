name: expense_web
description: "Personal Finance App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  fl_chart: ^0.69.0
  intl: ^0.19.0
  firebase_core: ^3.6.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.3.1
  firebase_storage: ^12.1.0
  go_router: ^14.2.0
  provider: ^6.0.0
  hive: ^2.2.3
  syncfusion_flutter_pdf: ^31.1.19
  path_provider: ^2.1.0
  file_picker: ^10.3.2
  bank_statement_parser:
    git:
      url: git@github.com:manu-dev-mob/bank_statement_parser.git

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.4.0
  hive_generator: ^2.0.0

flutter:

  uses-material-design: true
